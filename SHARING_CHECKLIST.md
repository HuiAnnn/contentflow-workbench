# 下载运行验收记录

验收日期：2026-09-10。

## 本地已验证

在不包含原开发环境的全新源码目录中执行，运行环境为 macOS / Apple Silicon / Node.js 24.19.0：

- `npm ci` 成功从锁文件安装依赖。
- `npm run setup:local` 成功构建，并在空的本地 D1 数据库中应用全部 4 个迁移。
- 本地登录由开发服务提供 cookie，不需要原站点账户；未登录请求和伪造身份请求被拒绝。
- 商品新建、字段确认、模板版本生成、提交审核、审核通过、发布到本系统商品页全部通过。
- 服务停止并重新启动后，完整商品数据和发布快照保持一致。
- 多组 API 连接可以保存、选择和删除；接口响应不包含明文 Key。
- 真实 Workers 运行时中的请求拦截测试验证了 OCR、文案使用所选 Key，旧版本加密记录升级后仍可使用。
- 对已有数据重复初始化，没有更换加密密钥，没有改变商品记录，也没有重复执行迁移。
- TypeScript 类型检查通过。

## 模型测试范围

流程验收使用模板内容，模型运行时测试使用拦截响应与测试凭证，未消耗真实服务商额度。真实 OCR 与文案生成仍需使用者配置自己的有效 Key、接口地址和模型，并在其网络环境中测试。限流、账户余额、模型权限与服务商可用性不由源码包保证。

## 分享范围

分享副本只包含源码、依赖锁文件、迁移、组件及示例商品图片。已排除原站点项目 ID、原 Git 提交历史、原 Git 远程地址、个人运行数据库、真实模型 Key、本地加密密钥、依赖目录、构建目录和日志。

GitHub Actions 另设 Linux 与 Windows 的安装和流程检查；各次远程检查结果以仓库 Actions 页面为准。本地版本仅用于本人电脑的固定工作区，不提供公网多人身份验证。
